[Unit]
Description=ADOS WiFi Client Manager (ground-station profile)
After=NetworkManager.service
PartOf=ados-supervisor.service
Requires=NetworkManager.service
StartLimitIntervalSec=0

[Service]
# Join the shared accounting slice so MemoryCurrent/CPUUsage are
# observable per unit (the slice sets MemoryAccounting=yes).
Slice=ados.slice
Type=simple
User=root
ExecStart=/opt/ados/venv/bin/python -m ados.services.ground_station.wifi_client_manager
Restart=always
RestartSec=3
Environment=ADOS_CONFIG=/etc/ados/config.yaml
EnvironmentFile=-/etc/ados/env

AmbientCapabilities=CAP_NET_ADMIN CAP_NET_RAW
NoNewPrivileges=no
ProtectSystem=strict
ReadWritePaths=/var/ados /run/ados /etc/ados /var/lock

MemoryMax=64M

StandardOutput=journal
StandardError=journal
SyslogIdentifier=ados-wifi-client

[Install]
WantedBy=ados-supervisor.service
