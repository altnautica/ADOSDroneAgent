id: ados.usb-camera-uvc
display_name: "USB UVC camera"
transport: usb
match:
  regex: "/dev/video[0-9]+"
capabilities:
  - camera
  - uvc
extra:
  description: "Generic UVC-class USB camera, discovered via /dev/video*."
  category: camera
