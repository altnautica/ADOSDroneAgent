"""Ground-station UI helpers.

The panel is rendered by the native display service and the front-panel GPIO
buttons are read by the native input arbiter. This package keeps the
touch-calibration transform the display REST routes read.
"""
