"""Rasterized 24 px tab-bar icons.

PNG bytes live in this package directory; ``ados.services.ui.chrome.icons``
loads them at runtime via ``importlib.resources``. Source SVGs are
under ``assets/lcd-icons/``; refresh the PNGs by running
``scripts/render-lcd-icons.py``.
"""

from __future__ import annotations
