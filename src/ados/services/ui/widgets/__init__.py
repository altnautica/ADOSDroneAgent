"""Reusable widget primitives for LCD pages.

Empty for now. Real widgets (large-number tiles, sparkline cards,
drag-scroll list, video frame, gesture overlays) land in the
follow-up commits where the pages that need them ship.
"""

from __future__ import annotations
